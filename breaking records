a = [3,5,8,2,4,19,1,4]

high = []
low = []
temp1 = a[0]
temp2 = a[0]
high.append(a[0])
low.append(a[0])

for i in a:
    if i > temp1:
        high.append(i)
        temp1 = i
    if i < temp2:
        low.append(i)
        temp2 = i

#print(high)
#print(low)

result = print(f"{len(high)-1} {len(low)-1}")

print(type(result))

